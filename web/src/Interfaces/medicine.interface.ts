export default interface Medicine {
    id: string;
    name: string;
    unit: string;
    total: number;
}

export const medicine: Medicine = {
    id: '',
    name: '',
    unit: '',
    total: 0,
}